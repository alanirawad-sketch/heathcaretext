[package]
name = "healthcaretext"
version = "0.1.0"
edition = "2021"
description = "CLI tool for healthcare-specific text editing workflows"
license = "MIT"

[dependencies]
clap = { version = "4.4", features = ["derive"] }
regex = "1"
